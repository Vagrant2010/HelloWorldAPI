spring:
  application:
    name: HelloWorldAPI
  main:
    banner-mode: off
  output:
    ansi:
      enabled: always

server:
  port: 8085
  servlet:
    context-path: /API

spring-doc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: method